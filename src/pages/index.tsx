import { Button } from "components/ui";
import { classNames } from "utils";
import cl from "./home.module.scss";

const Home = () => {
    return (
        // <Button variant="NavigateHome" className={cl.button}>
        //     Кнопка
        // </Button>
        <h1>Work</h1>
    );
};

export default Home;
